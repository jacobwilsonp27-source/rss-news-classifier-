# ─────────────────────────────────────────────────────────────────────────────
# Education News Module – requirements.txt
# Python 3.14+  |  install: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# Web framework & ASGI server
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# RSS parsing
feedparser>=6.0.11

# Hugging Face AI models
transformers>=4.41.0
torch>=2.3.0           # CPU-only; swap for torch+cu121 for GPU
sentencepiece>=0.2.0   # required by some tokenisers
accelerate>=0.30.0     # efficient model loading

# HTTP (used internally by feedparser / transformers)
requests>=2.32.0

# Async support utilities (optional but recommended)
httpx>=0.27.0
